/** @type {import('next').NextConfig} */
module.exports = {};
// const nextConfig = {
//   reactStrictMode: true,
// };
//
// export default nextConfig;

// module.exports = {
//   webpack: (config) => {
//     config.module.rules.forEach((rule) => {
//       if (rule.test && rule.test.toString().includes('css-loader')) {
//         rule.options.sourceMap = true;
//         rule.options.importLoaders = 1;
//         rule.options.modules = true;
//         rule.options.esModule = true;
//       }
//     });
//     return config;
//   },
// };